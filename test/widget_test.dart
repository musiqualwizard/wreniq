// Wreniq widget tests
// Full integration tests require mocked providers and SharedPreferences.
// Add feature-specific tests here as the app grows.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — project compiles', () => expect(true, isTrue));
}
